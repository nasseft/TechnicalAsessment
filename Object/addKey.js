// Create a function called addKey that takes in an object and adds a new key
// name weight and assigns it a value. Return the object.

var animal=(weight=100);
var addKey;
let obj = " "
function addKey(obj) {
return obj.weight;
}

